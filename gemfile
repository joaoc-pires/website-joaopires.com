source "https://rubygems.org"

# Jekyll core
gem "jekyll"

group :jekyll_plugins do
  # Theme plugin (if not already included in the repo)
  # gem "jekyll-theme-onepage"
  
  # Commonly used Jekyll plugins
  gem "jekyll-sitemap"
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-paginate"
  gem "jekyll-archives"
end
